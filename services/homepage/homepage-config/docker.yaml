---
# For configuration options and examples, please see:
# https://gethomepage.dev/configs/docker/

# Docker integration intentionally disabled: the socket is not mounted into
# the Homepage container (see ../homepage.yml). See issue #36.
