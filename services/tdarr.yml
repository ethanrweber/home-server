services:
  tdarr:
    container_name: tdarr-server
    image: ghcr.io/haveagitgat/tdarr:latest
    restart: unless-stopped
    network_mode: bridge
    ports:
      - 8265:8265 # webUI port
      - 8266:8266 # server port
    environment:
      - TZ=America/Chicago
      - PUID=1000
      - PGID=1000
      - UMASK_SET=002
      - serverIP=192.168.1.2 # I'm using my local ip; cmd > ipconfig
      - serverPort=8266
      - webUIPort=8265
      - internalNode=true # run the node as part of the server container rather than separately
      - inContainer=true
      - ffmpegVersion=6
      - nodeName=internal-node
      - NVIDIA_DRIVER_CAPABILITIES=all
      - NVIDIA_VISIBLE_DEVICES=all
    volumes:
      - ${CONFIG_ROOT}/Tdarr/server:/app/server
      - ${CONFIG_ROOT}/Tdarr/configs:/app/configs
      - ${CONFIG_ROOT}/Tdarr/logs:/app/logs
      - ${CONFIG_ROOT}/Tdarr/transcode_cache:/temp # note from tutorial - it's preferable to have the transcode cache on an ssd
      - ${MEDIA_ROOT}/tvshows:/home/Tdarr/Media/TVShows
      - ${MEDIA_ROOT}/movies:/home/Tdarr/Media/Movies
    devices:
      - /dev/dri:/dev/dri
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: all
              capabilities: [gpu]
